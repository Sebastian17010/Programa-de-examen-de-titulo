# Definición de propiedades principales
wms.type=universal
wms.language=es
wms.system=activo
wms.program=wayup
wms.platform=multi
wms.version=1.0

# Propiedades de tipo
wms.integer=100
wms.bool=true

# Mensajes del sistema
system.updated.message=El sistema está actualizado
system.design.message=El sistema no está en diseño
system.ready.message=The system is ready
system.notready.message=The system is not ready

# Acciones de base de datos
sql.select=SELECT * FROM database
sql.update=UPDATE system SET status='updated'

# Acciones de programa
program.import=import system.program.class
program.update=system.program.public
program.print=system.out.println

# Ejemplo de acción
update.system.program.class=system.void.println

# Parámetros de codificación
program.entrypoint=Main
program.classname=WayupWMS
program.methodname=actualizarSistema
program.status=pendiente

# Ejemplo de configuración de conexión
db.host=localhost
db.port=3306
db.user=admin
db.password=admin123
db.name=wms_db

# Configuración de logs
log.level=INFO
log.file=logs/wms.log

# Configuración de usuario
user.default=admin
user.language=es
